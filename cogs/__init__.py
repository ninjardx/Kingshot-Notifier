# cogs/__init__.py

# Optional: Use this to re-export shared helpers or config constants
# so other files can do: `from cogs import BEAR_CHANNEL` etc.

# Example (optional only):
# from .config import load_config, save_config, CONFIG_PATH, REACTION_CHANNEL, BEAR_CHANNEL, BEAR_LOG_CHANNEL, ARENA_CHANNEL
# from .helpers import ensure_channel, ensure_role

# Otherwise, leave it empty.